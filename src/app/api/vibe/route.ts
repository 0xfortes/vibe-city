import { NextResponse } from 'next/server';

export async function GET() {
  // Phase 2+: Vibe score endpoints
  return NextResponse.json({ data: { message: 'Vibe endpoint — not yet implemented' } });
}
